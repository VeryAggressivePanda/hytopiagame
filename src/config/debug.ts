export const DEBUG_RAFT = false;            // set true to log raft debug info
export const DEBUG_RAFT_INTERVAL = 20;     // ticks between logs when enabled
export const DEBUG_COLLIDERS = true;      // renders collider outlines (global)
export const DEBUG_JUMP = true;           // logs jump/ground state when enabled
